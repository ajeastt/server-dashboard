services:
  server-dashboard:
    image: ghcr.io/ajeastt/server-dashboard:latest
    container_name: server-dashboard
    ports:
      - "${SERVERDASH_PORT:-3001}:3001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    environment:
      - PORT=3001
